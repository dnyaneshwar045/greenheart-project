# Golf Charity Frontend

React + Vite app with:

- Member portal (auth, subscription, score tracking, charity view)
- Admin portal (KPIs, users, subscriptions, donations, draw runner)

## Run

1. Start API: `cd ../backend && npm start`
2. Start UI: `npm run dev`

API base URL used by frontend: `http://localhost:3001`
